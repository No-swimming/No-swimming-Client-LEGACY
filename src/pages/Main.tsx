import Search from "../components/Search";
import * as _ from "../styles/Main";

const Main = () => {
  return (
    <_.Container>
      <_.Logo />
      <Search />
    </_.Container>
  );
};

export default Main;
