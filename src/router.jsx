import { BrowserRouter, Route, Routes } from "react-router-dom";
const Router = () => {
  return (
    <BrowserRouter>
      <Routes>{/* <Route path="/asd" element={} /> */}</Routes>
    </BrowserRouter>
  );
};

export default Router;
